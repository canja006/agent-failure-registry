Add under an existing "Related work" / "Ecosystem" heading, or at the end of the
README. Keep it to these lines — a longer entry reads as promotion.

```markdown
### Cross-taxonomy mapping

`afr-mapping.yaml` maps Agent-Xray's 22 cascade categories onto the vendor-neutral
`AF-####` ids used by the [Agent Failure Registry][afr], so Agent-Xray output can
be compared with reports from other diagnostic tools. The file is
informational — Agent-Xray does not read it and takes no dependency on it.

[afr]: https://github.com/YOURNAME/agent-failure-registry
```

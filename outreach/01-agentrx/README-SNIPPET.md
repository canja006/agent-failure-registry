Add under an existing "Related work" / "Ecosystem" heading, or at the end of the
README. Keep it to these lines — a longer entry reads as promotion.

```markdown
### Cross-taxonomy mapping

`afr-mapping.yaml` maps AgentRx's root-cause labels onto the vendor-neutral
`AF-####` ids used by the [Agent Failure Registry][afr], so AgentRx output can
be compared with reports from other diagnostic tools. The file is
informational — AgentRx does not read it and takes no dependency on it.

[afr]: https://github.com/canja006/agent-failure-registry
```
